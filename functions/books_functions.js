// There are functions to verify and check things like the current year etc.
// You could write that function to check validity of your other function.
// the console.error( ); tells you if there's an error

var book1 = function(title, writer, printYear) {
console.error();

  var book = {
    name: title,
    author: writer,
    year: printYear

  };
  // if (book.year
  return book;

}

console.log(book1("Harry Potter", "J.K.Rowling", 1991));
